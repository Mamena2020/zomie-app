import '/Router/Middleware/Middleware.dart';
import '/Router/Middleware/MiddlewareCore.dart';

class MiddlewareConfig {
  static List<Middleware> list() {
    return [];
    // return [Middleware(params: MiddlewareParams(status: status, params: params))];
  }
}
